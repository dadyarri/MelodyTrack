# Keep browser requests on the Vite origin so LAN devices can use the dev proxy.
VITE_API_BASE_URL=/api

# Backend address reachable from the development machine running Vite.
# The proxy preserves /api, so Backend must use the development /api path base.
MELODY_TRACK_API_PROXY_TARGET=http://localhost:5000
